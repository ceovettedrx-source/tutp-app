node_modules/
/data/
.env
.DS_Store
# Generated by `npm run build` (tailwindcss) — the Dockerfile rebuilds this
# during the image build, so it shouldn't be committed/drift from source.
/public/css/tailwind.css
